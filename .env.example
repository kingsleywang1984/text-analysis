# Example environment for local development/testing.
# Copy to `.env` and fill in values as needed.
#
# Notes:
# - This project requires many vars because `load_config()` validates constraints strictly.
# - Leave LLM_BASE_URL/LLM_API_KEY/LLM_MODEL empty if you want to run without a real LLM.

# ----------------------------
# Embedding
# ----------------------------
EMBEDDING_PROVIDER=tfidf  # tfidf | openai
EMBEDDING_MODEL=tfidf
EMBEDDING_API_BASE_URL=https://api.openai.com
EMBEDDING_API_KEY=
EMBEDDING_API_TIMEOUT_SECONDS=30
EMBEDDING_API_BATCH_SIZE=256
EMBEDDING_TFIDF_MAX_FEATURES=2048
EMBEDDING_TFIDF_NGRAM_MIN=1
EMBEDDING_TFIDF_NGRAM_MAX=2

# ----------------------------
# Clustering
# ----------------------------
CLUSTER_SIMILARITY_THRESHOLD=0.55
CLUSTER_MAX_CLUSTERS=10
CLUSTER_OVERFLOW_STRATEGY=OTHER  # OTHER | DROP

# ----------------------------
# Sentiment (VADER thresholds)
# ----------------------------
SENTIMENT_STRONG_NEGATIVE_THRESHOLD=-0.5
SENTIMENT_POSITIVE_THRESHOLD=0.3
SENTIMENT_NEGATIVE_THRESHOLD=-0.2

# ----------------------------
# LLM output budgets
# ----------------------------
CLUSTER_INSIGHTS_MIN=2
CLUSTER_INSIGHTS_MAX=3

COMPARISON_SIMILARITIES_MIN=1
COMPARISON_SIMILARITIES_MAX=3
COMPARISON_DIFFERENCES_MIN=1
COMPARISON_DIFFERENCES_MAX=3

# ----------------------------
# LLM client config (OpenAI-compatible)
# ----------------------------
LLM_PROVIDER=none  # none | openai_compatible
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=

LLM_TIMEOUT_SECONDS=8
LLM_TEMPERATURE=0.2
LLM_MAX_RETRIES=1

# LLM-first bounded controls (updated-doc alignment)
LLM_MAX_CLUSTERS=10
LLM_REPRESENTATIVE_SENTENCES_PER_CLUSTER=10
